"use strict";
{
    //1. baseck or old systed type declor 
    const arrayString = ['s', 'e', 'd'];
    //2. normal stander
    const arrayString2 = ['s', 'e', 'd'];
    //3. normal stander short formet use 
    const arrayString3 = ['s', 'e', 'd'];
    //4. main stander 
    const arrayString4 = ['s', 'e', 'd'];
    //4. main stander  use 
    const userData = [
        {
            userName: 'sujon',
            old: 10,
            home: 'Rangpur'
        },
        {
            userName: 'sujon',
            old: 10,
            home: 'ra'
        },
        {
            userName: 'sujon Admin',
            old: 10,
            home: 'Rangpur',
            role: 'admin'
        }
    ];
    // genarik tupol 
    // fast system
    const manePluse = ['male', 'female'];
    const manePluse2 = ['male', 'female'];
    // example 
    const userInfo = [150, { name: 'sujon', home: 'Rangpur', role: 'admin', phone: 179087652 }];
    // const userInfo: ManpluseTopol<number, { name: string; home: string; role: string; phone: number }> = [
    //     150,
    //     { name: 'sujon', home: 'Rangpur', role: 'admin', phone: 179087652 }
    // ];
    // baseck or old systed type declor 
    const arrayNumber = [2, 5000, 2, 14];
    // normal stander
    const arrayNumber2 = [2, 5000, 2, 14];
    // baseck or old systed type declor 
    const arrayBoolean = [true, true, false];
    // normal stander
    const arrayBoolean2 = [true, true, false];
    // dynamic create type 
}
