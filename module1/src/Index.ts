let myName = "sujon mia"
console.log(myName)
{
    // inston of gsrd
    // insinanceof operator
    class Animol {
        name: string;
        spaces: string;

        constructor(name: string, spaces: string) {
            this.name = name;
            this.spaces = spaces;
        }

        makeSound(makeCreateSound: string) {
            console.log(makeCreateSound);
        }
    }

    class Dog extends Animol {
        constructor(name: string, spaces: string) {
            super(name, spaces);
        }
        makeEtingtime(time: number) {
            // console.log(time)
        }
    }
    class Dog extends Animol {
        constructor(name: string, spaces: string) {
            super(name, spaces);
        }
        makeEtingtime(time: number) {
            console.log(time);
        }
    }





    // 
}
